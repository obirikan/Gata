import React from 'react'
import Form from '../Components/TalkToUs/Form';
import PageWrapper from '../Layout/PageWrapper';

const TalkToUs = () => {
    return (
        <PageWrapper>
            <Form/>
        </PageWrapper>
      );
}

export default TalkToUs